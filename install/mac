mkdir -p ~/.config
mkdir -p ~/.local
mkdir -p ~/Library
mkdir -p ~/Library/Application\ Support
mkdir -p ~/Library/Application\ Support/jesseduffield

ln -s ~/.dotfiles/alacritty ~/.config/alacritty
ln -s ~/.dotfiles/bin ~/.local/bin
ln -s ~/.dotfiles/lazygit ~/Library/Application\ Support/jesseduffield/lazygit
ln -s ~/.dotfiles/nvim ~/.config/nvim
ln -s ~/.dotfiles/tmux ~/.config/tmux
ln -s ~/.dotfiles/zsh ~/.config/zsh
ln -s ~/.dotfiles/zshrc ~/.zshrc
ln -s ~/.dotfiles/Brewfile ~/.Brewfile

source ~/.zshrc

for FONT in ~/.dotfiles/fonts/*
do
  cp $FONT ~/Library/Fonts
done

for SCRIPT in ~/.local/bin/*
do 
  if [ -f $SCRIPT ]; then
    chmod +x $SCRIPT
  fi
done

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew bundle install --global

curl -LO https://invisible-island.net/datafiles/current/terminfo.src.gz && gunzip terminfo.src.gz
/usr/bin/tic -xe alacritty-direct,tmux-256color terminfo.src
rm terminfo.src
